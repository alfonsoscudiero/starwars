<nav class="navbar navbar-expand-lg navbar-dark bg-dark border-top border-secondary shadow-sm">
  <div class="container">

    <!-- Logo / Brand -->
    <a class="navbar-brand d-flex align-items-center gap-2" href="/">
      <img
        src="/img/logo/starwars-app-logo.webp"
        alt="Star Wars API"
        width="44"
        height="44"
        class="d-inline-block align-text-top"
      >
    </a>

    <!-- Right-side controls (Login + Toggle) -->
    <div class="d-flex align-items-center gap-2 order-lg-2">

      <% if (!user) { %>
        <a href="/login" class="btn btn-outline-warning btn-sm px-3">
          Login
        </a>
      <% } else { %>
        <a href="/logout" class="btn btn-warning btn-sm px-3">
          Logout
        </a>
      <% } %>
    </div>

  </div>
</nav>